{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "production": "npx cypress run --headed  --browser edge --config baseUrl=\"https://www.facebook.com\"",
    "staging": "npx cypress run --headed  --browser edge --config baseUrl=\"https://www.google.com\"",
    "local": "npx cypress run --headed  --browser chrome --config baseUrl=\"https://www.yahoo.com\"",
    "canary": "npx cypress run --headed  --browser chrome --config baseUrl=\"https://www.youtube.com\"",
    "configS": "npx cypress run --headed --config-file staging-config.json",
    "configp": "npx cypress run --headed --config-file production-config.json",
    "configc": "npx cypress run --headed --config-file canary-config.json",
    "configl": "npx cypress run --headed --config-file local-config.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.7.0",
    "npx": "^10.2.2"
  },
  "devDependencies": {
    "cypress-file-upload": "^5.0.8",
    "cypress-xpath": "^2.0.1",
    "node-xlsx": "^0.21.0"
  }




  
}
